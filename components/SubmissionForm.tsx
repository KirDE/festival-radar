"use client";
import { FormEvent, useState } from "react";
export function SubmissionForm() {
  const [message, setMessage] = useState("");
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setMessage("Submitting…"); const form=event.currentTarget; try { const response=await fetch("/api/submissions",{method:"POST",headers:{"content-type":"application/json","accept":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))}); const contentType=response.headers.get("content-type")??""; const result=contentType.includes("application/json")?await response.json():null; if(!response.ok||!result?.accepted){setMessage(result?.error||`Submission failed (HTTP ${response.status}).`);return;} setMessage(`${result.duplicate?"Already received":"Received for review"} (${result.reference}).`); form.reset(); } catch { setMessage("Network error. Nothing was claimed as received; please try again."); } }
  return <form className="submissionForm" onSubmit={submit}><label>Festival name<input name="name" required minLength={2} maxLength={100}/></label><label>Official source URL<input name="officialUrl" type="url" required/></label><label>Edition year<input name="year" type="number" min="2027" max="2100" required/></label><label>Notes<textarea name="notes" maxLength={1000}/></label><input className="honeypot" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true"/><button type="submit">Send for editorial review</button><p role="status" aria-live="polite">{message}</p></form>;
}
