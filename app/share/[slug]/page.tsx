import { notFound } from "next/navigation";
import { db } from "@/lib/db";
export const dynamic = "force-dynamic";
export default async function SharedPlan({ params }: { params: Promise<{ slug: string }> }) { const link = await db.shareLink.findUnique({ where: { slug: (await params).slug }, include: { document: true } }); if (!link || (link.expiresAt && link.expiresAt <= new Date())) notFound(); const payload = link.document.payload as { attendance?: Record<string, string> }; return <div className="plannerPage"><div className="plannerHero"><div><div className="eyebrow">PUBLIC FESTIVAL PLAN</div><h1>Shared plan</h1><p>Last updated {link.document.updatedAt.toLocaleString("en-GB", { timeZone: "UTC" })} UTC</p></div></div><section className="plannerSection"><h2>Festival attendance</h2>{Object.keys(payload.attendance || {}).length ? <ul>{Object.entries(payload.attendance || {}).map(([festival, status]) => <li key={festival}><strong>{festival}</strong> — {status}</li>)}</ul> : <p>No attendance selections have been shared yet.</p>}</section></div>; }
